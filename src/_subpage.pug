doctype html
html
    head
        include _head.pug
        block head
    body
        header.sub: a(href='./')
            img(src='images/logo.svg' width='40px' height='30px')
            p SEEK IT!
        main
            block main
        footer
            include _footer.pug
